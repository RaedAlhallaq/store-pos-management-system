import { Clock, PauseCircle, PlayCircle, ShoppingBag, Trash2, User } from 'lucide-react';
import { Button } from '../../../components/ui/Button';
import { Modal } from '../../../components/ui/Modal';

export function HeldOrdersModal({ isOpen, onClose, heldOrders, onResumeOrder, onDeleteOrder }) {
  return <Modal isOpen={isOpen} onClose={onClose} title="الفواتير المعلقة مؤقتاً" subtitle={`يوجد (${heldOrders.length}) فواتير معلقة بانتظار الاستئناف`} maxWidth="md"><div className="space-y-3">{heldOrders.length === 0 ? <div className="text-center py-8 text-slate-500"><PauseCircle className="w-10 h-10 mx-auto mb-2 text-slate-600" /><p className="text-sm font-semibold">لا توجد أي فواتير معلقة حالياً</p></div> : heldOrders.map((order) => <div key={order.id} className="p-4 rounded-2xl bg-slate-950/80 border border-slate-800 flex items-center justify-between gap-3 hover:border-slate-700 transition-colors"><div className="space-y-1"><div className="flex items-center gap-2 text-xs text-slate-400"><Clock className="w-3.5 h-3.5 text-brand-400" /><span>{order.timestamp}</span></div><div className="flex items-center gap-2"><User className="w-3.5 h-3.5 text-slate-500" /><span className="text-sm font-bold text-slate-100">{order.customerName || 'عميل نقدي'}</span></div><div className="flex items-center gap-2 text-xs text-slate-400"><ShoppingBag className="w-3.5 h-3.5 text-slate-500" /><span>{order.items.length} أصناف</span><span className="text-brand-400 font-mono font-bold">({order.total.toFixed(2)} ₪)</span></div></div><div className="flex items-center gap-2"><Button variant="ghost" size="sm" onClick={() => onDeleteOrder(order.id)} className="text-slate-500 hover:text-rose-400 hover:bg-rose-500/10 p-2" title="حذف الفاتورة المعلقة"><Trash2 className="w-4 h-4" /></Button><Button size="sm" onClick={() => { onResumeOrder(order); onClose(); }} className="bg-brand-500 hover:bg-brand-600 text-slate-950 font-bold" rightIcon={<PlayCircle className="w-4 h-4" />}>استئناف</Button></div></div>)}<div className="flex justify-end pt-3 border-t border-slate-800"><Button variant="outline" onClick={onClose}>إغلاق</Button></div></div></Modal>;
}
